// ==================== Telegram WebApp ====================
const tg = window.Telegram?.WebApp;
if (tg) { tg.ready(); tg.expand(); }

// Кнопки на головному екрані поки статичні (демо-макет), логіку додамо наступним кроком.
